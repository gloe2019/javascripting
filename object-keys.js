const car = {
  make: "Honda",
  model: "Accord",
  year: 2020,
};
//Object.keys(obj_name) allows you list all the avaiable keys of an object. this could be useful for looping though properties and manipulating their values...
const keys = Object.keys(car);
console.log(keys);
